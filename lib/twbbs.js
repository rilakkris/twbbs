'use strict';

const Telnet = require('./telnet');
const Terminal = require('./terminal');

class TwBBS {
  constructor(options) {
    super(options);
  }

}

if (!module.parent) {
  console.log(Telnet);
}
